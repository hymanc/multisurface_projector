#ifndef  _COLOR_GEN_HPP
#define  _COLOR_GEN_HPP

#include <opencv2/core/core.hpp>
#include <opencv2/highgui/highgui.hpp>
#include <time.h>

#define BLACK_SPACE     8
#define COLOR_BAR_WIDTH 2

class colorGenerator{

	private:

	int width;
	int height;
	String filename;

	void MyLine( Mat img, int x, Scalar color, int thickness);


	public:

	void colorEncoder(int w, int h, String fname;
	void colorEncoder(int w, int h);
	void saveToFile()
	void setWidth(int w);
	void setHeigth(int h);
	void setFilename(String fname);
	void displayWindow(String winName);
}
#endif
